import { Request, Response } from "express";

export const getAvailableBalance = (req: Request, res: Response) => {
  // Lógica para obtener el saldo disponible del empleado
};

export const requestWageAccess = (req: Request, res: Response) => {
  // Lógica para procesar la solicitud de acceso a los salarios
};
